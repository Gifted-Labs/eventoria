spring:
  datasource:
    username: ${DATABASE_USERNAME_DEV}
    password: ${DATABASE_PASSWORD_DEV}
    url: ${DATABASE_URL_DEV:jdbc:postgresql://127.0.0.1:5432/eventsdb}

app:
  jwtSecret: ${JWT_SECRET_DEV}

server:
  port: 5000